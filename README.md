# invoice-generator
A Python tool that **auto-generates professional PDF invoices from client data**. It handles invoice numbers, line items, tax, and totals automatically — producing clean, branded invoices in seconds.
